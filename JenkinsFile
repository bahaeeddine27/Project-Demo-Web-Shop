pipeline {

    agent {
        docker {
            image 'maven:3.9-eclipse-temurin-17'
            args '--shm-size=2g -u root'
        }
    }

    parameters {
        booleanParam(
            name: 'GENERATE_ALLURE',
            defaultValue: true,
            description: 'Générer le rapport Allure HTML'
        )
    }

    stages {

        stage('Préparation') {
            steps {
                sh '''
                    echo "===== Installation de Chrome ====="

                    apt-get update
                    apt-get install -y wget gnupg ca-certificates

                    wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | \
                    gpg --dearmor -o /usr/share/keyrings/google-chrome.gpg

                    echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome.gpg] http://dl.google.com/linux/chrome/deb/ stable main" \
                    > /etc/apt/sources.list.d/google-chrome.list

                    apt-get update
                    apt-get install -y google-chrome-stable

                    echo "===== Versions ====="
                    google-chrome --version
                    mvn --version

                    echo "===== Projet ====="
                    ls -la
                    ls -la demo
                    ls -la demo/pom.xml
                '''
            }
        }

        stage('Tests Selenium') {
            steps {
                dir('demo') {
                    sh '''
                        mvn clean test
                    '''
                }
            }
        }

        stage('Génération Allure HTML') {
            when {
                expression {
                    params.GENERATE_ALLURE
                }
            }

            steps {
                dir('demo') {
                    sh '''
                        echo "===== Résultats Allure ====="
                        ls -la target/allure-results || true
                    '''
                }
            }
        }
    }

    post {

        always {

            echo '===== Publication Surefire ====='

            junit(
                testResults: 'demo/target/surefire-reports/*.xml',
                allowEmptyResults: true
            )

            echo '===== Archivage des résultats ====='

            archiveArtifacts(
                artifacts: 'demo/target/surefire-reports/**,demo/target/allure-results/**',
                allowEmptyArchive: true
            )

            script {

                if (params.GENERATE_ALLURE) {

                    echo '===== Publication du rapport Allure ====='

                    allure(
                        includeProperties: false,
                        jdk: '',
                        results: [
                            [path: 'demo/target/allure-results']
                        ]
                    )
                }
            }
        }

        success {
            echo 'Les tests Selenium sont terminés avec succès.'
        }

        failure {
            echo 'Les tests Selenium ont échoué.'
        }
    }
}